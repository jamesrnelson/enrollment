<%= render partial: "nav" %>

<h1>This is a list of all students</h1>

<% @students.each do |student| %>
  <p> <%= link_to student.name, student_path(student) %> - <%= link_to "Delete", student_path(student), method: "delete" %> </p>
<% end %>
